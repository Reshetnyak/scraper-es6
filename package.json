{
  "name": "scraper",
  "version": "1.0.0",
  "description": "Scraping properties data from real estate web sites",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src_scripts -w -d compiled -s"
  },
  "keywords": [
    "scrapers"
  ],
  "author": "Denis Reshetniak",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
